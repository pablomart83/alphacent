# AlphaCent strategy catalog — category: vix_regime
# 2 authored templates. AUTHORED form (pre-normalization).
# Generated by Phase 1 extraction; edit by hand thereafter.
- seq: 93
  name: High-VIX Mean Reversion Long
  description: Buy when RSI < 30 and price below lower Bollinger Band in high-volatility regimes (VIX
    > 25 proxy). Oversold bounces are sharper when fear is elevated. Tight 2% stop, exit at RSI > 50.
  strategy_type: mean_reversion
  market_regimes:
  - ranging_high_vol
  - trending_down_weak
  entry_conditions:
  - RSI(14) < 30 AND CLOSE < BB_LOWER(20, 2)
  exit_conditions:
  - RSI(14) > 50
  required_indicators:
  - RSI
  - Bollinger Bands
  default_parameters:
    rsi_period: 14
    rsi_entry: 30
    rsi_exit: 50
    bb_period: 20
    bb_std: 2
    stop_loss_pct: 0.02
    take_profit_pct: 0.06
  expected_trade_frequency: 2-4 trades/month
  expected_holding_period: 3-10 days
  risk_reward_ratio: 2.5
  metadata:
    direction: long
    regime_preference:
    - ranging_high_vol
    - trending_down_weak
    strategy_category: vix_regime
    volatility_proxy: high_vix
    vix_equivalent: '>25'
- seq: 94
  name: Low-VIX Trend Following Long
  description: Buy breakouts above upper Bollinger Band with RSI > 50 momentum confirmation in low-volatility
    regimes (VIX < 15 proxy). Low-vol trends persist longer. Wider 4% stop, exit below SMA(20).
  strategy_type: trend_following
  market_regimes:
  - trending_up
  - ranging_low_vol
  - trending_up_strong
  - trending_up_weak
  entry_conditions:
  - CLOSE > BB_UPPER(20, 2) AND RSI(14) > 50
  exit_conditions:
  - CLOSE < SMA(20)
  required_indicators:
  - Bollinger Bands
  - RSI
  - SMA
  default_parameters:
    bb_period: 20
    bb_std: 2
    rsi_period: 14
    rsi_entry: 50
    sma_period: 20
    stop_loss_pct: 0.04
    take_profit_pct: 0.08
  expected_trade_frequency: 1-3 trades/month
  expected_holding_period: 10-30 days
  risk_reward_ratio: 2.0
  metadata:
    direction: long
    regime_preference:
    - trending_up
    - ranging_low_vol
    strategy_category: vix_regime
    volatility_proxy: low_vix
    vix_equivalent: <15
